// ? External repository
import Validation from "../../submodules/ValidationJS/js/Validation.js";

document.addEventListener('DOMContentLoaded', e => {
    // TODO: Contact validation
    
    // validation.mail.contact.validation = new Validation({
    //     id: 'contact-form',
    // }, {
    //     ignore: ['g-recaptcha-response']
    // }, validation.mail.contact.rules,
    // validation.mail.contact.messages);
});